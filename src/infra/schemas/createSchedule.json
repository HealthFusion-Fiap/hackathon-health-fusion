{
  "$id": "create-schedule",
  "required": [
    "startAt",
    "endAt"
  ],
  "properties": {
    "startAt": {
      "type": "string",
      "format": "date-time",
      "errorMessage": {
        "format": "O horário de início deve ser válido."
      }
    },
    "endAt": {
      "type": "string",
      "format": "date-time",
      "errorMessage": {
        "format": "O horário de término deve ser válido."
      }
    }
  }
}